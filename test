#!/bin/bash

set -e

ticks=6000
team1=my
team2=old

for seed in `seq 100 149`
do
    echo -n "$seed "
    java -jar dist/russianaicup.jar $team1 $team2 $ticks $seed
done

for seed in `seq 150 199`
do
    echo -n "$seed "
    java -jar dist/russianaicup.jar $team2 $team1 $ticks $seed
done
